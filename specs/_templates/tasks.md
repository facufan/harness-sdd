# Tasks — «name»

> Copia esta plantilla a `specs/<name>/tasks.md`. Pasos discretos en orden;
> cada task termina en `Cubre: R<n>[, R<m>]`. Toda R<n> debe quedar cubierta
> por al menos una task (check-spec.sh lo valida). El implementer marca `[x]`.
> Tipos: bug → T1 es el test de regresión que FALLA en rojo antes del arreglo;
> refactor → T1 es asegurar tests de caracterización verdes antes de mover nada.

- [ ] T1 — «cambio concreto en un archivo». Cubre: R1.
- [ ] T2 — «test "nombre del test" en la ubicación de tests del área». Cubre: R1.
- [ ] T3 — «...». Cubre: R2.
